VALID_EMAIL_REGEX: !ruby/regexp /\A[\w+\-.]+@[a-z\d\-.]+\.[a-z]+\z/i
CHECKBOX_TRUE: "1"
email_maximum: 255
name_maximum: 50
password_minimum: 6
image_type:
  - jpg
  - jpeg
  - gif
  - png
size_picture: 400
image_type_accept: image/jpeg,image/gif,image/png
image_size_accept: 5
avata_default: avatar.jpg
size_picture_display: 100x100
size_picture_display_small: 50x50
size_picture_display_icon: 30x30
users_per_index: 10
categoies_per_page: 3
following_type: "following"
follow_type:
  - following
  - followers
words_per_page: 20
query:
  word_not_learn: category_id = :category_id AND id NOT IN (SELECT word_id FROM lesson_words WHERE lesson_id IN (SELECT id FROM lessons where category_id = :category_id AND user_id = :user_id))
  learned: "category_id = :category_id AND id IN (SELECT word_id from lesson_words where lesson_id in (select id from lessons where category_id = :category_id AND user_id = :user_id))"
words_per_lesson: 20